---
title: 'Templates de Composição'
description: 'Crie, liste, copie e publique templates completos do editor'
---

Templates de composição são documentos JSON portáteis do editor. Eles podem definir a
geometria do vídeo, textos, imagens, vídeos auxiliares, formas, thumbnails, barra de
progresso, animações, legendas, fundo, filtro de cor e um snapshot de Brand Kit.

Eles são diferentes dos [Caption Templates](/api-reference/caption-templates), que
alteram apenas o estilo das legendas enviado em `captionTemplate`.

Todas as rotas abaixo exigem Bearer token. Em workspaces de equipe, envie também
`X-Workspace-Id`; os templates privados pertencem ao workspace ativo. Veja a
[orientação de Workspaces](/api-reference/workspaces).

## Endpoints

| Método | Endpoint | Resultado |
|---|---|---|
| `GET` | `/templates/mine` | Lista os templates do workspace ativo |
| `GET` | `/templates/public` | Lista a galeria publica aprovada |
| `GET` | `/templates/{id}` | Retorna um template próprio ou público aprovado |
| `POST` | `/templates` | Cria um template privado |
| `POST` | `/templates/{id}/copy` | Copia um template visível para o workspace |
| `PATCH` | `/templates/{id}` | Atualiza um template do workspace |
| `DELETE` | `/templates/{id}` | Exclui um template do workspace |
| `POST` | `/templates/{id}/publish` | Publica na galeria |
| `POST` | `/templates/{id}/unpublish` | Remove da galeria publica |
| `POST` | `/templates/{id}/use` | Registra uso de forma deduplicada |

<Warning>
A API de templates gerencia e distribui os documentos do editor. O contrato atual de
`POST /createShorts` não aceita apenas um ID de template de composição. Não confunda esse
recurso com o campo `captionTemplate`.
</Warning>

## Listar meus templates

```bash
curl "https://api.realoficial.com.br/api/v1/templates/mine" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Accept: application/json"
```

A resposta não é paginada:

```json
{
  "items": [
    {
      "id": "01KZTEMPLATE00000000000001",
      "user_id": "01KZUSER000000000000000001",
      "source_template_id": null,
      "forked_from_seed_id": null,
      "name": "Podcast Vertical",
      "description": "Vídeo central e título no topo",
      "data": {
        "schemaVersion": 1,
        "format": "vertical",
        "composition": { "width": 1080, "height": 1920 },
        "elements": [],
        "meta": { "name": "Podcast Vertical" }
      },
      "cover_url": null,
      "format": "vertical",
      "is_public": false,
      "published_at": null,
      "moderation_status": "approved",
      "use_count": 0,
      "schema_version": 1,
      "created_at": "2026-08-13T12:00:00+00:00",
      "updated_at": "2026-08-13T12:00:00+00:00",
      "author": {
        "id": "01KZUSER000000000000000001",
        "name": "Minha marca"
      },
      "forked_from": null
    }
  ]
}
```

IDs são ULIDs opacos; trate-os sempre como strings.

## Galeria pública

```bash
curl "https://api.realoficial.com.br/api/v1/templates/public?sort=popular&page=1&pageSize=24&q=podcast" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Accept: application/json"
```

| Query | Tipo | Padrão | Regra |
|---|---|---|---|
| `format` | string | omitido | Atualmente somente `vertical` retorna resultados |
| `sort` | string | `popular` | `popular` ou `recent` |
| `q` | string | vazio | Busca por nome |
| `page` | integer | `1` | Mínimo 1 |
| `pageSize` | integer | `24` | Entre 1 e 60 |

```json
{
  "items": [],
  "page": 1,
  "pageSize": 24,
  "total": 0,
  "hasMore": false
}
```

Somente templates com `is_public: true`, `moderation_status: "approved"` e formato
vertical aparecem na galeria.

## Criar template

```bash
curl -X POST "https://api.realoficial.com.br/api/v1/templates" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "name": "Podcast Vertical",
    "description": "Vídeo principal ocupando a composição",
    "format": "vertical",
    "data": {
      "schemaVersion": 1,
      "format": "vertical",
      "composition": { "width": 1080, "height": 1920 },
      "elements": [
        {
          "id": "main-video",
          "kind": "video-slot",
          "rect": { "x": 0, "y": 0, "width": 100, "height": 100 },
          "zIndex": 0,
          "crop": "face-track",
          "mask": "none"
        }
      ],
      "meta": {
        "name": "Podcast Vertical",
        "description": "Vídeo principal ocupando a composição"
      }
    }
  }'
```

A criação retorna o record completo com HTTP 201. O template nasce privado,
`moderation_status: "approved"` e `use_count: 0`.

<ParamField body="name" type="string" required>
  Entre 1 e 120 caracteres. URLs não são aceitas no nome.
</ParamField>

<ParamField body="description" type="string">
  Até 2000 caracteres.
</ParamField>

<ParamField body="format" type="string" required>
  Atualmente deve ser `vertical`.
</ParamField>

<ParamField body="data" type="object" required>
  Documento completo no schema v1, limitado a 256 KB e 200 elementos.
</ParamField>

<ParamField body="cover_url" type="string">
  URL HTTPS opcional em um host de mídia permitido.
</ParamField>

<ParamField body="forked_from_seed_id" type="string">
  ID de um template-base do frontend quando a criação deriva desse modelo.
</ParamField>

## Schema do documento v1

```json
{
  "schemaVersion": 1,
  "format": "vertical",
  "composition": { "width": 1080, "height": 1920 },
  "elements": [],
  "animations": [],
  "captionsEnabled": true,
  "captionsZone": {
    "rect": { "x": 5, "y": 72, "width": 90, "height": 15 }
  },
  "captionsStyle": {},
  "background": { "kind": "none" },
  "colorFilter": { "filterId": "none", "intensity": 0 },
  "meta": {
    "name": "Nome do template",
    "description": "Descrição opcional",
    "coverUrl": "https://cdn.realoficial.com.br/capa.png"
  }
}
```

As coordenadas de `rect` são percentuais da composição.

| `kind` | Função |
|---|---|
| `text` | Texto livre ou ligado ao título do corte com `bindToCutTitle` |
| `shape` | Retângulo ou círculo |
| `image` | Imagem, logo ou avatar |
| `video` | Vídeo auxiliar enviado pelo usuário |
| `video-slot` | Posição e recorte do vídeo principal |
| `thumbnail-slot` | Thumbnail do corte ou imagem estática |
| `progress` | Barra de progresso sincronizada |
| `animation` | Preset de animação nativo do editor |

O `video-slot.crop` aceita `face-track`, `center`, `cover`, `square`, `contain` e
`stretch`. O documento pode ter no máximo um `video-slot`.

Fundos aceitos:

```json
{ "kind": "none" }
{ "kind": "solid", "color": "#111111" }
{ "kind": "gradient", "css": "linear-gradient(180deg, #111111, #333333)" }
{ "kind": "image", "src": "https://cdn.realoficial.com.br/fundo.png", "fit": "cover", "blur": 4 }
{ "kind": "blur-video", "blur": 10 }
```

URLs de mídia devem usar HTTPS e um destes hosts exatos:

- `brandkit.realoficial.com.br`
- `cdn.realoficial.com.br`
- `editor-uploads.realoficial.com.br`

## Copiar e editar

Para copiar um template próprio ou um template público aprovado:

```bash
curl -X POST "https://api.realoficial.com.br/api/v1/templates/TEMPLATE_ID/copy" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Content-Type: application/json" \
  -d '{"name":"Podcast Vertical - cópia"}'
```

`name` e `description` são opcionais. A cópia recebe um novo ULID, nasce privada,
zera `use_count` e referencia a origem em `source_template_id`.

`PATCH /templates/{id}` aceita qualquer subconjunto de `name`, `description`, `data`,
`cover_url` e `format`. Se `data` for enviado, o documento completo é revalidado.

## Publicar e despublicar

```bash
curl -X POST "https://api.realoficial.com.br/api/v1/templates/TEMPLATE_ID/publish" \
  -H "Authorization: Bearer seu_token_aqui"
```

Publicar exige uma assinatura ativa e revalida todo o documento. Para remover o
template da galeria sem exclui-lo, use `POST /templates/{id}/unpublish`.

## Registrar uso

`POST /templates/{id}/use` retorna HTTP 204. O contador ignora usos do próprio
workspace e contabiliza no máximo um uso por usuário/template a cada hora. Rajadas acima
de cinco chamadas por minuto também retornam 204 sem incrementar.

## Erros importantes

| Status | Situação |
|---|---|
| `403` | Sem permissão para gerenciar projetos ou sem assinatura para publicar |
| `404` | Template inexistente, privado de outro workspace ou indisponível |
| `422` | Payload, documento, nome ou URL de mídia inválida |
