---
title: 'Meus Shorts Copiados'
api: 'GET /lives/my-shorts'
description: 'Lista shorts que o usuário copiou de lives'
---

## Endpoint

```
GET https://api.realoficial.com.br/api/v1/lives/my-shorts
```

<Note>
Este endpoint requer autenticação via Bearer Token.
</Note>

Lista todos os shorts que o usuário copiou de lives (20 por página). Inclui informações do short original e da live de origem.

## Query Parameters

<ParamField query="page" type="integer">
  Número da página (padrão: 1)
</ParamField>

<RequestExample>
```bash cURL
curl -X GET "https://api.realoficial.com.br/api/v1/lives/my-shorts" \
  -H "Authorization: Bearer seu_token_aqui"
```
</RequestExample>
