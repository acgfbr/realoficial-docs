---
title: 'Analytics'
description: 'KPIs, desempenho de posts, crescimento e exportação de dados sociais'
---

Os endpoints de Analytics combinam métricas sincronizadas das contas sociais com dados de
projetos e shorts da Real Oficial.

## Filtros comuns

<ParamField query="range" type="integer">
  Janela em dias: `30`, `60` ou `90`. Valores diferentes usam o padrão 30.
</ParamField>

<ParamField query="platforms" type="string | array">
  Plataformas separadas por vírgula (`instagram,tiktok`) ou como array de query string.
  Quando omitido, inclui todas.
</ParamField>

<ParamField query="social_account_ids" type="string | array">
  IDs separados por vírgula ou como array. Também é aceito o singular
  `social_account_id`. Máximo de 100 IDs.
</ParamField>

## Endpoints principais

Todos usam o prefixo `/dashboard/analytics`.

| Endpoint | Resultado |
|---|---|
| `GET /overview` | KPIs de views, shorts, minutos e origem dos dados |
| `GET /posts` | Lista paginada de posts e métricas |
| `GET /timeseries` | Série temporal do período |
| `GET /top-posts` | Posts de melhor desempenho |
| `GET /platforms` | Comparação por plataforma |
| `GET /accounts` | Comparação por conta social |
| `GET /best-time` | Horários de melhor desempenho |
| `GET /fatigue` | Sinais de fadiga de conteúdo |
| `GET /follower-growth` | Crescimento de seguidores |
| `GET /youtube/post/{socialPostId}/daily` | Views diárias no YouTube |
| `GET /instagram/demographics` | Demografia do Instagram |
| `GET /content-decay` | Decaimento de desempenho |
| `GET /repurpose-suggestions` | Sugestões de conteúdo para reaproveitar |
| `GET /hashtag-trends` | Tendencias de hashtags |
| `GET /competitor-benchmark` | Benchmark de concorrentes |
| `GET /posts/{socialPostId}/sentiment` | Sentimento dos comentários |
| `GET /export` | Exportação CSV |

## Exemplos

```bash
curl \
  "https://api.realoficial.com.br/api/v1/dashboard/analytics/overview?range=30&platforms=instagram,tiktok" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Accept: application/json"
```

```json
{
  "views": {
    "today": 1200,
    "yesterday": 980,
    "total": 45200,
    "delta_pct": 22.45
  },
  "shorts": {},
  "minutes": {},
  "source_breakdown": {},
  "range_days": 30,
  "platforms": ["instagram", "tiktok"],
  "social_account_ids": [],
  "last_synced_at": "2026-08-13T12:00:00.000000Z"
}
```

### Posts

`GET /posts` aceita:

- `sortBy`: `date`, `views`, `impressions`, `likes`, `comments`, `shares` ou
  `engagement`;
- `order`: `asc` ou `desc`;
- `page`: padrão 1;
- `limit`: entre 1 e 100, padrão 20.

### Top posts

`GET /top-posts` aceita `metric` (`views`, `impressions`, `likes`, `comments`, `shares`
ou `engagement_rate`) e `limit` entre 1 e 50.

### Melhor horário

`GET /best-time` aceita `metric` (`views`, `likes` ou `engagement_rate`) e `scope`
(`user` ou `global`).

### Demografia do Instagram

`GET /instagram/demographics` exige `social_account_id` e aceita `metric` como
`follower_demographics` ou `engaged_audience_demographics`.

### Sugestões de reaproveitamento

`GET /repurpose-suggestions` aceita `min_views` (padrão 5000), `age_days_min` (mínimo 7,
padrão 30) e `limit` (máximo 50, padrão 20).

## Exportar CSV

```text
GET /dashboard/analytics/export?type=posts&range=30
```

`type` pode ser `posts`, `platforms` ou `accounts`. A resposta usa
`Content-Type: text/csv` e inicia o download do arquivo.

## Metas

| Método | Endpoint | Descrição |
|---|---|---|
| `GET` | `/dashboard/analytics/goals` | Lista metas |
| `POST` | `/dashboard/analytics/goals` | Cria meta |
| `PUT` | `/dashboard/analytics/goals/{id}` | Atualiza meta |
| `DELETE` | `/dashboard/analytics/goals/{id}` | Arquiva meta |

Ao criar uma meta:

- `metric`: `views`, `followers`, `posts` ou `engagement_rate`;
- `scope`: `total`, `platform` ou `account`;
- `period`: `weekly`, `monthly`, `quarterly` ou `custom`;
- `target_value`, `starts_at` e `ends_at` são obrigatórios.

<Note>
As métricas sociais dependem da sincronização com os provedores. Consulte
`last_synced_at`; uma resposta vazia pode significar que a conta ainda não possui
snapshots no período solicitado.
</Note>
