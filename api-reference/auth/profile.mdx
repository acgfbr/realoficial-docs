---
title: 'Atualizar Perfil'
api: 'POST /profile'
description: 'Atualize dados do seu perfil, incluindo a URL de webhook'
---

## Endpoint

```
POST https://api.realoficial.com.br/api/v1/profile
```

<Note>
Este endpoint requer autenticação via Bearer Token.
</Note>

<Warning>
Este endpoint aceita apenas `POST`. Uma requisição `GET` retorna `405 Method Not Allowed`
— para **ler** os dados do perfil, use [Dados do Usuário](/api-reference/auth/user).
</Warning>

Todos os campos são opcionais: envie apenas os que quiser alterar. Campos ausentes do body
permanecem inalterados.

## Body Parameters

<ParamField body="name" type="string">
  Nome do usuário. Máximo 255 caracteres.
</ParamField>

<ParamField body="nickname" type="string">
  Apelido/nome de exibição. Máximo 255 caracteres.
</ParamField>

<ParamField body="email" type="string">
  Email do usuário. Precisa ser único e ter formato válido. Máximo 255 caracteres.
</ParamField>

<ParamField body="phone" type="string">
  Telefone. Máximo 20 caracteres.
</ParamField>

<ParamField body="tax_id" type="string">
  CPF ou CNPJ. Máximo 20 caracteres.
</ParamField>

<ParamField body="pix_key" type="string">
  Chave PIX usada para recebimentos. Máximo 255 caracteres.
</ParamField>

<ParamField body="ignore_notifications" type="boolean">
  Desliga o envio de notificações por email.
</ParamField>

<ParamField body="webhook_url" type="string">
  URL que receberá os eventos de webhook. Envie `null` ou string vazia para desativar.

  Veja [Webhooks](#webhooks) abaixo para as restrições de URL.
</ParamField>

<Note>
Campos não listados aqui — como `balance`, `token`, `read_only_token` ou os campos de cota
— **não são editáveis** por este endpoint, mesmo aparecendo no `GET /user`. Enviar esses
campos não produz erro; eles simplesmente são ignorados.
</Note>

## Webhooks

Ao definir `webhook_url`, a Real Oficial passa a enviar `POST` com JSON para essa URL
quando um projeto fica pronto (`project.ready`) ou uma renderização termina
(`render.completed`).

A URL passa por validação de segurança e precisa:

- usar esquema `http` ou `https`;
- apontar para um host público resolvível.

URLs apontando para endereços internos/privados são rejeitadas com `422`.

<Warning>
Trate a URL de webhook como um **segredo**. Ela é o único fator que autentica as
notificações recebidas — quem souber a URL consegue enviar payloads forjados para o seu
endpoint. Prefira uma URL longa e com componente aleatório, e valide o conteúdo recebido
antes de agir sobre ele.
</Warning>

## Resposta de Sucesso

<ResponseField name="message" type="string">
  Mensagem de confirmação
</ResponseField>

<ResponseField name="user" type="object">
  O objeto do usuário já com os dados atualizados.
</ResponseField>

<RequestExample>
```bash cURL
curl -X POST "https://api.realoficial.com.br/api/v1/profile" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "webhook_url": "https://meu-app.com/webhooks/realoficial/SEGREDO"
  }'
```

```javascript JavaScript
const response = await fetch('https://api.realoficial.com.br/api/v1/profile', {
  method: 'POST',
  headers: {
    'Authorization': 'Bearer seu_token_aqui',
    'Content-Type': 'application/json',
    'Accept': 'application/json'
  },
  body: JSON.stringify({
    webhook_url: 'https://meu-app.com/webhooks/realoficial/SEGREDO'
  })
});

const { user } = await response.json();
```

```python Python
import requests

response = requests.post(
    'https://api.realoficial.com.br/api/v1/profile',
    headers={
        'Authorization': 'Bearer seu_token_aqui',
        'Content-Type': 'application/json',
        'Accept': 'application/json'
    },
    json={'webhook_url': 'https://meu-app.com/webhooks/realoficial/SEGREDO'}
)

data = response.json()
```
</RequestExample>

<ResponseExample>
```json 200 - Sucesso
{
  "message": "Perfil atualizado com sucesso.",
  "user": {
    "id": "01kqk0kev5w02yv89148zyr3dx",
    "name": "Maria Silva",
    "email": "maria@exemplo.com",
    "webhook_url": "https://meu-app.com/webhooks/realoficial/SEGREDO"
  }
}
```

```json 422 - Webhook Inválido
{
  "message": "A URL de webhook informada não é válida.",
  "errors": {
    "webhook_url": ["A URL de webhook informada não é válida."]
  }
}
```

```json 422 - Email Duplicado
{
  "message": "The email has already been taken.",
  "errors": {
    "email": ["The email has already been taken."]
  }
}
```
</ResponseExample>

## Desativando o webhook

Envie `null` (ou string vazia) para parar de receber notificações:

```bash
curl -X POST "https://api.realoficial.com.br/api/v1/profile" \
  -H "Authorization: Bearer seu_token_aqui" \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{ "webhook_url": null }'
```
